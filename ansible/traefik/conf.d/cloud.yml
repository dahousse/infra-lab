http:
  routers:
    cloud:
      rule: "Host(`cloud.mysmihome.duckdns.org`)"
      entryPoints:
        - websecure
      service: cloud
      tls:
        certResolver: letsencrypt
  services:
    cloud:
      loadBalancer:
        servers:
          - url: "http://192.168.1.11:80"
