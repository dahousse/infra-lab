http:
  routers:
    uptime:
      rule: "Host(`uptime.mysmihome.duckdns.org`)"
      entryPoints:
        - websecure
      service: uptime
      tls:
        certResolver: letsencrypt
  services:
    uptime:
      loadBalancer:
        servers:
          - url: "http://192.168.1.35:3001"
